//Operadores aritmeticos
fun main() {
    
    val numero_mantenimientos_echos=10
    val costo_por_mantenimiento=35
    println("numero_mantenimientos_echos: ${numero_mantenimientos_echos}")
    println("costo_por_mantenimiento: ${costo_por_mantenimiento}")
    
  
    println("numero_mantenimientos_echos==costo_por_mantenimiento ${numero_mantenimientos_echos==costo_por_mantenimiento}")
    println("numero_mantenimientos_echos!=costo_por mantenimineto ${numero_mantenimientos_echos!=costo_por_mantenimiento}")
   
    println("numero_mantenimientos_echos < costo_por_mantenimiento = ${numero_mantenimientos_echos < costo_por_mantenimiento}")
    println("numero_mantenimientos_echos >= costo_por_mantenimiento = ${numero_mantenimientos_echos >= costo_por_mantenimiento}")
    println("numero_mantenimientos_echos <= costo_por_mantenimiento = ${numero_mantenimientos_echos <= costo_por_mantenimiento}")
    println("numero_mantenimientos_echos > costo_por_mantenimiento = ${numero_mantenimientos_echos > costo_por_mantenimiento}")
    
    val string1 = "Mantenimiento"
    val string2 = "Costo"
    println("stringl===string2 ${string1===string2}")
	println("stringl==string2 ${string1==string2}")
    println("stringl.equals(string2) ${string1.equals(string2)}")
    }
    
    
    